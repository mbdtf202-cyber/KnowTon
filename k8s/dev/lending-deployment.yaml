apiVersion: apps/v1
kind: Deployment
metadata:
  name: lending-service
  namespace: knowton-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lending-service
  template:
    metadata:
      labels:
        app: lending-service
    spec:
      containers:
      - name: lending-service
        image: ghcr.io/knowton/lending-service:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "development"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: connection-string
        - name: ARBITRUM_RPC_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: ARBITRUM_RPC_URL
        - name: PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: wallet-secret
              key: private-key
        - name: LENDING_ADAPTER_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: contract-addresses
              key: LENDING_ADAPTER_ADDRESS
        - name: COPYRIGHT_REGISTRY_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: contract-addresses
              key: COPYRIGHT_REGISTRY_ADDRESS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: lending-service
  namespace: knowton-dev
spec:
  selector:
    app: lending-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lending-service-hpa
  namespace: knowton-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lending-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
